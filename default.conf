server {
    listen 80;
    server_name ristotoivanen.com www.ristotoivanen.com;

    root /usr/share/nginx/html;
    index index.html;

    location = /readmore {
        try_files /readmore.html =404;
    }

    location / {
        try_files $uri /index.html;
    }
}